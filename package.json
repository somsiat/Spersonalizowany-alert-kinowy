{
  "name": "kino-alert-supabase-adapted",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @kino/web run dev",
    "dev:api": "npm --workspace @kino/api run dev",
    "dev:worker": "npm --workspace @kino/worker run dev",
    "build:web": "npm --workspace @kino/web run build",
    "build:api": "npm --workspace @kino/api run build",
    "build:worker": "npm --workspace @kino/worker run build"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
